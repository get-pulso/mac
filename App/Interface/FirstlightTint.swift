import SwiftUI

extension Color {
    /// Firstlight's violet: the arrival page's #8262FF → #6644F2 pill resolved
    /// to one tint. Every prominent control in the app takes this instead of
    /// the system accent, so the button that opens a tray reads as the same
    /// brand as the button that signed you in.
    static let firstlight = Color(red: 0.45, green: 0.33, blue: 0.97)
}
